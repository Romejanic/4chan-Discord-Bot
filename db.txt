table SERVER_CONFIG:
    id: server snowflake id
    ---------------------------
    default_board: varchar(10),
    prefix: varchar(10),
    restricted: bool,
    removal_time: int

table SERVER_ALLOWED_CHANNELS:
    server: server snowflake id
    channel: channel snowflake id